import { CutToLengthPipe } from './cut-to-length.pipe';

describe('CutToLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new CutToLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
